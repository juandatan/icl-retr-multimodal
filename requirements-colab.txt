# Colab-specific requirements
# Note: Colab already has torch and torchvision with CUDA support pre-installed
# We skip torch/torchvision to avoid overwriting the CUDA-enabled versions

# Multimodal models
transformers>=4.37.0
accelerate>=0.25.0
bitsandbytes>=0.41.0  # For 8-bit/4-bit quantization

# CLIP for embeddings
openai-clip
Pillow>=9.0.0

# VQA dataset handling
datasets>=2.14.0
pycocotools>=2.0.6

# Utilities
# Note: Colab uses numpy 2.x, and PyTorch 2.0+ is compatible with it
numpy>=2.0.0
pandas>=2.0.0
tqdm>=4.65.0
scikit-learn>=1.3.0
matplotlib>=3.5.0  # For visualization

# Configuration management
pyyaml>=6.0
hydra-core>=1.3.0

# Logging
wandb>=0.15.0  # Optional, for experiment tracking
